# แตกไฟล์ zip ที่ดาวน์โหลดไว้
cd noodle-island-repo-full

# ตั้งค่า git repo แล้ว push
git init
git remote add origin https://github.com/CAIN147/noodle-island.git
git add .
git commit -m "🎮 Initial push with full project (game + PWA + build APK)"
git branch -M main
git push -u origin main
